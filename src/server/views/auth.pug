
doctype html
html
  head
    meta(charset='UTF-8')
    meta(http-equiv='x-ua-compatible' content='IE=edge')
    meta(name='viewport', content='width=device-width, initial-scale=1, shrink-to-fit=no')
    title #{title}
    link(href='//developer.ringcentral.com/assets/images/favicon.ico?v=20181214-0738', type='image/vnd.microsoft.icon', rel='shortcut icon')
  body
    #bg
    #container
    #content-loading(style='text-align:center;padding:30px;font-size:24px') Simulating a auth process...
    script.
      window.rc = !{JSON.stringify(_global)}
    script.
      if (window.opener) {
        console.log('999')
        setTimeout(function() {
          console.log('99w9',  window.opener.postMessage)
          window.opener.postMessage({
            authDone: true
          }, window.rc.server)
          window.close()
        }, 1000)
      }
